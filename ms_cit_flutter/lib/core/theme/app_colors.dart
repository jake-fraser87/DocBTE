import 'package:flutter/material.dart';

/// Central color tokens for the app. Keeping these in one place means
/// every screen (and every ViewModel-driven status color) stays in sync.
class AppColors {
  AppColors._();

  static const Color orange = Color(0xFFEA6A2E);
  static const Color orangeDark = Color(0xFFD64C1F);
  static const Color orangeSoft = Color(0xFFFDEDE3);

  static const Color blue = Color(0xFF2F6FED);
  static const Color blueSoft = Color(0xFFEAF1FF);

  static const Color amber = Color(0xFFE8912B);
  static const Color amberSoft = Color(0xFFFBEBD3);

  static const Color green = Color(0xFF1FA855);
  static const Color greenSoft = Color(0xFFE4F6EA);

  static const Color red = Color(0xFFE5484D);
  static const Color redSoft = Color(0xFFFCE7E7);

  static const Color teal = Color(0xFF14967A);
  static const Color tealSoft = Color(0xFFE1F4F0);

  static const Color ink = Color(0xFF1D1B18);
  static const Color inkSoft = Color(0xFF6B6660);
  static const Color line = Color(0xFFE7E2DB);
  static const Color paper = Color(0xFFFBFAF7);
  static const Color pageBg = Color(0xFFE9E5DD);
}
