name: ms_cit_verification
description: MS-CIT Certificate Verification — admin app (MVVM)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  # MVVM state management: ViewModels extend ChangeNotifier,
  # Views listen via Provider/Consumer/context.watch.
  provider: ^6.1.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
